# slice03 — corpus-map (slice doc)

## Goal

Synthesize the two closed, verified discovery slices (slice01 Code, slice02
Desktop) into **`../corpus-map.md`** — the single map arc02's normalized schema is
written against. Pure synthesis of already-reproduced findings; no new data pass.

## Seats

**Doer = CDC** (synthesis). Slice03's correctness *is* arc01's composition (does the
map let arc02 draft its schema without re-spelunking?), so its independent
verification folds into the **arc01 close gate** (operator or fresh context /
subagent), rather than a separate CC pass.

## Scope

**In:** sources roster (both kinds × roots/accounts, coverage gaps); per-source
schema condensation; cross-source reconciliation → the normalized-message sketch;
the provenance-dimension → field mapping; consolidation of the arc02/arc03 open
questions; the committed `corpus-map.md` (schema/design only, no personal data).

**Out:** the actual arc02 schema, dedup/identity/canonical-path decisions, any code.

## Verification approach

The map's §3b **A-6 demonstration** — a normalized record drafted citing *only* the
map — is the reproducible evidence that arc02 is draftable without re-opening the
raw corpus. The arc01 independent gate confirms A-4/A-5/A-6 against the artifacts.

## Exit criteria

Ledger closed; `corpus-map.md` complete; arc01 composition rows (A-4/A-5/A-6)
satisfiable from the map. This is arc01's last slice → the arc close follows.
